spring.application.name=employee.management
# ===============================
# = Spring Boot Configurations =
# ===============================

# App Port
server.port=8989

# ====================
# = Database Configs =
# ====================
spring.datasource.url=jdbc:mysql://localhost:3306/techm?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Johnabraham777#

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
server.error.whitelabel.enabled=false

# ====================
# = JPA Logging (Dev)=
# ====================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ======================
# = Security (Optional)=
# ======================
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN

# ==================
# = Actuator & Dev =
# ==================
management.endpoints.web.exposure.include=*
 Database Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/techm?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Johnabraham777#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
